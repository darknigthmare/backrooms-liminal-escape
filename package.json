{
  "name": "backrooms-liminal-escape",
  "version": "3.0.0",
  "private": true,
  "description": "Survival-horror liminal original, génératif et jouable hors ligne.",
  "type": "module",
  "scripts": {
    "check": "node --test tests/*.test.mjs && node tools/check.mjs",
    "test": "npm run check",
    "build": "npm run check && node tools/build.mjs",
    "start": "node tools/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
